# 贡献指南

感谢你愿意让胸外科手术运营分析看板变得更好。修正一个错别字、补充一种字段别名、改善手机端布局，都是有价值的贡献。

这是一个纯静态 HTML/JavaScript 项目，不需要安装依赖或配置后端。只要有新版浏览器和 Python 3，就可以在本地开始。

## 先选择你要做什么

| 你想做的事 | 建议入口 |
| --- | --- |
| 报告页面或数据处理问题 | 选择“报告 Bug” Issue 表单 |
| 建议新图表、字段或工作流程 | 选择“建议新功能” Issue 表单 |
| 修改文档或小问题 | 可以直接提交 Pull Request |
| 开发较大功能或引入依赖 | 先建 Issue 讨论方向，避免重复工作 |
| 报告漏洞、敏感数据或社区行为问题 | 使用“申请私下联系”表单，不要公开细节 |

所有参与者都需遵守 [社区行为准则](CODE_OF_CONDUCT.md)。

## 第一次贡献，可以从这里开始

- 把一段难懂的说明改得更通俗，或修正错别字和失效链接。
- 补充常见的 Excel/CSV 字段别名，但测试数据必须完全合成。
- 改善手机端阅读、键盘操作、颜色对比度或图表提示。
- 增加更清楚的空数据、错误数据和导入失败提示。
- 用少量合成数据记录一个可重现的边界情况。

如果你在 Issue 列表中看到 `good first issue` 标签，那些问题会尽量保持范围小、背景清楚，适合第一次参与。

## 最重要的规则：不要上传真实医疗数据

GitHub 上的 Issue、提交记录和 Pull Request 都可能长期公开保留。请不要提交：

- 患者或医务人员真实姓名。
- 住院号、门诊号、病案号、检查号、电话或地址。
- 可识别特定个人的诊断、手术、日期和转归组合。
- 医院内部文件、系统地址、账号、令牌或其他凭据。

需要演示问题时，请使用看板自带的合成数据，或自己构造少量例如“医生01”的测试数据。如果发现已经泄露的敏感内容，请按 [安全政策](SECURITY.md) 处理，不要转发。

## 本地开发

### 1. 获取代码

如果你不是仓库协作者，先在 GitHub 页面点击 **Fork**，再克隆自己的 Fork：

```bash
git clone https://github.com/<your-username>/surgery-dashboard.git
cd surgery-dashboard
```

然后从最新的 `main` 创建一个短小、聚焦的分支：

```bash
git switch main
git pull --ff-only
git switch -c fix/short-description
```

建议使用 `fix/`、`feature/`、`docs/` 或 `refactor/` 作为分支前缀。

### 2. 启动看板

在项目根目录运行：

```bash
python3 -m http.server 8000
```

然后打开 [http://localhost:8000](http://localhost:8000)。页面会从 CDN 加载 ECharts 等资源，所以首次打开时需要联网。

### 3. 做一次聚焦的修改

- `index.html` 包含页面结构和界面内容。
- `app.js` 包含数据解析、统计、筛选和图表逻辑。
- `README.md` 面向使用者，请保持通俗，尽量不假设读者懂编程。

优先沿用现有写法和交互方式。如果需要引入新依赖、更改数据口径或大幅调整界面，请先建 Issue 说明动机和方案。

## 怎样验证修改

只需测试与你的改动相关的部分，并在 Pull Request 中如实说明。

| 改动类型 | 建议检查 |
| --- | --- |
| 只修改文档 | 链接、拼写、Markdown 显示和内容准确性 |
| 样式或布局 | 宽屏和手机窄屏，并确认关键操作仍可用 |
| 图表或筛选 | 默认演示数据、筛选联动、重置与无数据状态 |
| Excel/CSV 导入 | 合成的 Excel 和 CSV、空行、标题行、字段别名与错误提示 |
| 导出或分类 | 导出范围与当前筛选一致，人工分类后统计正确更新 |

所有代码改动都建议再检查两件事：

1. 浏览器控制台没有新的 JavaScript 错误。
2. 页面、截图、测试数据和 Git 差异中没有真实医疗信息。

## 提交 Pull Request

提交前先查看实际差异：

```bash
git status
git diff
```

只添加本次修改需要的文件，然后提交并推送分支：

```bash
git add <files-you-changed>
git commit -m "Describe the change briefly"
git push -u origin <your-branch>
```

创建 Pull Request 时：

- 用一两句话说清“修改了什么”和“为什么要改”。
- 勾选与本次改动相关的测试项；不适用的项可留空并简要说明。
- 界面有变化时可以附上前后对比图，但截图必须使用合成或脱敏数据。
- 尽量保持一个 PR 解决一个主要问题，这样更容易审查和回退。

维护者可能会询问场景、提出修改建议或请你补充测试。这是正常的协作过程，不代表你的贡献没有价值。

## 还有疑问？

如果不确定问题应该归到哪里，可以先阅读 [README](README.md) 和 [安全政策](SECURITY.md)，再选择最接近的 Issue 模板。对于不涉及隐私或安全的普通问题，欢迎清楚说明你想完成的事和已经尝试的方法。
