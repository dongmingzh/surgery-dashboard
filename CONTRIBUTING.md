# 贡献指南

感谢你关注胸外科手术运营分析看板！本项目是纯静态 HTML/JS 应用，Excel/CSV 数据仅在浏览器本地处理，无需后端或构建工具。

## 目录

- [行为准则](#行为准则)
- [开始之前](#开始之前)
- [贡献流程](#贡献流程)
- [代码规范](#代码规范)
- [提交自检清单](#提交自检清单)
- [隐私要求](#隐私要求)

## 行为准则

请阅读并遵守 [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)。所有贡献者都应营造友好、尊重和包容的社区环境。

## 开始之前

### 了解项目

- 项目结构见 [README.md](README.md#项目结构)
- 本看板定位：**运营汇总、工作量回顾、数据质控和可视化演示**，不是医疗器械，不用于临床诊断或治疗决策

### 找到可以贡献的方向

- 报告 Bug 或提出功能建议 → 使用 Issue 模板
- 代码优化、文档改进、新图表类型、国际化等 → 欢迎 Pull Request

## 贡献流程

### 1. 新建分支

从 `main` 分支签出，分支名建议使用以下前缀：

| 前缀 | 场景 |
| --- | --- |
| `feature/` | 新功能或新图表 |
| `fix/` | Bug 修复 |
| `docs/` | 文档改进 |
| `refactor/` | 代码重构（不改变功能） |
| `i18n/` | 国际化/本地化 |

示例：

```bash
git checkout main
git pull origin main
git checkout -b feature/add-weekly-trend-chart
```

### 2. 本地开发

本项目不需要安装依赖。直接在项目根目录启动一个静态文件服务器：

```bash
python3 -m http.server 8000
```

然后在浏览器中打开 [http://localhost:8000](http://localhost:8000)。

> 页面加载时依赖 CDN 资源（ECharts、Tailwind 等），需要联网。

### 3. 确认修改

修改代码后：

1. 在浏览器中强制刷新（Cmd+Shift+R / Ctrl+Shift+R）页面
2. 检查默认演示数据和全部图表是否正常显示
3. 检查浏览器控制台（Console）是否有 JavaScript 错误
4. 测试基本功能：导入 CSV/Excel、筛选联动、重置、导出
5. 在宽屏和窄屏（手机视口）下检查布局

### 4. 查看差异

在提交前，检查改动的文件范围：

```bash
git diff
```

确认没有意外修改、不应提交的调试代码或敏感信息。

### 5. 提交并推送

```bash
git add <需要提交的文件>
git commit -m "简明描述本次改动"
git push origin <分支名>
```

### 6. 创建 Pull Request

在 GitHub 上创建 PR，填写 PR 模板中的信息。

## 代码规范

- HTML/CSS/JS 保持无构建步骤，所有逻辑位于 `app.js`
- 变量和函数名使用 camelCase，中文注释
- 不引入额外依赖；如需新增第三方库，请在 Issue 中先讨论
- 图表配置保持与现有风格一致（ECharts 配置项、颜色、字体）

## 提交自检清单

提交 Pull Request 前，请逐项确认：

- [ ] 默认演示数据和全部图表正常显示，控制台无 JavaScript 错误
- [ ] Excel、CSV、空行、标题行和常见字段别名可以识别
- [ ] 日期、组别、医生、类别及关键词筛选联动正确
- [ ] 医疗组设置与人工分类修正触发全局重算
- [ ] 导出结果与当前筛选范围一致
- [ ] 宽屏和手机窄屏均可操作
- [ ] 代码、文档、截图和提交历史**不包含真实姓名或医疗标识符**
- [ ] 无调试代码、`console.log` 残留或未使用的变量

## 隐私要求

本项目处理的是医疗相关数据，隐私是最高优先级。请严格遵守：

- **永远不要**将真实患者数据、医务人员姓名或医院内部标识提交到仓库
- 演示数据 / 截图必须使用完全合成或脱敏的数据
- 提交截图前检查：图表标签、悬停提示（tooltip）、明细表行、浏览器下载文件名
- 上传至 GitHub 的任何文件都应是公开安全的
- 如发现仓库中意外包含敏感数据，**不要公开评论或转发**，请按 [SECURITY.md](SECURITY.md) 私下报告
